{
  "name": "@slaif-agent-site/browser-worker",
  "version": "0.0.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test": "pnpm --filter @slaif-agent-site/browser-tool-contracts build && pnpm run build && node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@slaif-agent-site/browser-tool-contracts": "workspace:0.0.0",
    "playwright-core": "1.62.1"
  },
  "devDependencies": {
    "@types/node": "25.9.5",
    "typescript": "7.0.2"
  }
}
