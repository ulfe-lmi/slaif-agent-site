# OAP Work Order — 078-k: close final component audit findings

## Authority and verified state

- Objective 078; increment 078/1; round 078-k; mode AMEND_EXISTING_PR.
- Existing PR [#77](https://github.com/ulfe-lmi/slaif-agent-site/pull/77);
  branch `oap/078-agent-composition-design-semantics`, base `main`.
- Verified main/base `ae3a4a681bb888260192b7bb1b2a337b4906828d`.
- Starting report head `6de7e088153b09cb0950d9b9b93e940ba535a4dd`,
  report-only child of `41b7f7d487c8f022537b34946e09c76a8a3fc7bf`.
- All 20 checks SUCCESS; cumulative 92 files, +24,270/-3,443.
- Read `oap/audits/078-1-final-hostile-audit.md`: strategy has performed the
  requested final whole-PR audit and rejected this revision for F1–F7.
- The human's bounded-PR amendment governs. This is defect closure only. No
  theme, new component breadth, global regions/header/footer or later feature.

## Repair the finite audit list

F1: The strategic direct runtime probe successfully persisted an undeclared
Button `alignment: {script: evil}`. 061's design validator strips alignment
unconditionally before the scalar validator. Fix full-document validation at
the trusted DB and Python boundaries: reject unknown properties for the exact
component, nested executable keys, JSON null/type errors and invalid maps
before collapsing any values. Supported alignment values require exact string
enums; JSON null must not pass SQL three-valued logic. Add public and direct
runtime regressions that assert unchanged props/version/accounting/COW state.

F2: Fully authorized responsive CREATE for Button, Image and CollectionGrid
returns 422 because 062 uses the scalar validator. Repair CREATE to use the
same full supported design schema as PATCH. Retain fixed L2 omitted defaults,
L2 supplied-design denial, all exact conditional scopes, and replay priority.
Prove all three responsive creates succeed with exact scopes and fail without
one; accepted creations must remain readable, movable, deletable and renderable.

F3: Actual Chromium measurements show advertised local design has no effect or
wrong fallback. Add the missing Section variant and responsive Spacer behavior,
correct fallback and reset handling, and ensure integer column/count tokens
cannot accept fractional values that the renderer silently changes. Inspect
ALL currently advertised scalar/responsive local-design choices for matching
trusted CSS. Test real browser computed styles at desktop/tablet/mobile,
including omitted labels and resets such as secondary->primary, fixed image
ratio->auto, and tablet override inherited by mobile according to the documented
cascade. Do not merely assert class strings. Use the production renderer/CSS
with existing Playwright; no new dependencies, theme, image bytes or feature
breadth. The diagnostic reference is
`/tmp/slaif-strategic-authority-3tLQVd/css.mjs`.

F4: Puck advertises responsive maps but legacy Editor SQL still uses scalar-only
validation. Make the existing human workspace Editor/Puck save/move/read path
preserve supported normalized local-design values using the same validator and
correct human property permissions. Prove valid authorized design save/reload
through production Editor HTTP plus Puck, and crafted missing-permission or
invalid input denial. This does NOT authorize exact-Agent-workspace selection
(081), new UI workflows or global design.

F5: 062 downgrade leaves its replaced UPDATE definition behind. Restore all
changed previous functions and grants exactly; compare `pg_get_functiondef`
and privilege signatures for UPDATE, CREATE and authority before/after.
Add a data-bearing downgrade/re-upgrade test. Preserve incompatible responsive
state safely when traversing 061->060: either supported backward reading or a
clear atomic downgrade refusal, never data loss. Use a new append-only repair
migration if needed; preserve frozen 060/catalog and historical OAP artifacts.

F6: Finish current truth reconciliation. MVP-PROGRESS and MVP-CONTRACT-AUDIT
must explicitly state Objective077/PR74 MERGED/accepted on 2026-09-08 at ae3a4a6,
not hedge that GitHub might have merged it. Separate pending PR77 component/local
design, remaining Objective078 theme/global/page-style/catalog work, and later
081+ objectives. Rewrite PR title/body around actual retained behavior and
verification, not round history. Keep current active exactly 078-k and numeric
078 PARTIAL. Update the increment ledger to awaiting strategic acceptance.

F7: Remove the two NEW whole-file Markdownlint ignores and the new repository-
policy allowlisting. They were not authorized, and the report's claim of hash
verification does not match the code. Format the mutable governance amendment
without changing its meaning. The extra final blank line in immutable 078-j is
a strategic publication error; strategy has requested a narrow human override
for that one newline only. Until that approval is relayed through a new
strategic control artifact, DO NOT edit 078-j or bypass its lint error. Complete
all technical repairs and checks that do not depend on that pending decision;
no fake green check. No other historical order/report changes.

## Evidence and bounded completion

Publish the strategic audit file unchanged. Add an executor-owned closure
evidence document under `oap/audits` mapping F1–F7 to actual code/tests/results;
preserve the original rejection and source head. Strategy will verify closure
and regressions without restarting a general audit or adding new features.

Retain the A/B/replay/no-effect fixes, narrowed L1/L2/L3 authority, quota/audit/
COW isolation, true concurrency barriers, cancellation, deployed restart,
catalog drift and exact OpenAPI. Run focused new regressions, existing affected
component tests once, applicable generated/quality/Node/docs and required CI.
Avoid unrelated/full repeated suites once relevant checks pass. Repair normal
in-scope failures within this turn; identify concrete external/decision blockers
if any. Routine setup belongs to the passwordless-sudo existing coder.

Do not add theme/later scope, new PR, dependency, security/test exception, or
merge/auto-merge. Commit exact order+active and governance/audit artifacts; push
to existing PR77. Final report
`oap/reports/078-k-close-final-component-audit.md` must be a report-only SELF
child of the literal implementation SHA. Include each F closure, exact tests
and checks, final size categories, preserved theme SHA, any pending human
newline decision, truth/safety and remaining increment scope. Signal exact FIFO
OK only after durable report publication, then wait.
