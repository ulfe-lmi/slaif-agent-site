# OAP Work Order — 078-y: finish the omitted 078-x proof

## Identity and verified baseline

Objective078, increment078/4, round078-y, AMEND_EXISTING_PR81
<https://github.com/ulfe-lmi/slaif-agent-site/pull/81>, SAME branch
`oap/078-4-page-style-overrides`. Base/currentmain
`fe31c9f30a7797d0916ad7f8fb56344bc61526f3`.
Verified report-only head `27a0a0deb00756749d027f85f49ca5a6007e94ea`, parent
`d2bc4fbee42fb78bf92f8c10ff93bc4bda681e67`; all20exact-head checks SUCCESS.
Genuine078-x response received; PR OPEN, not merged or accepted.

This is completion of omitted078-x substance, not a new semantic slice or
replanning of page-style. X made substantial valid progress: resource/version/
reset/OpenAPI repairs preserved, raw authority coverage improved, real post-DML
cancellation and fresh065 migration proof now exist. Strategy's independent
fresh065 diagnostic now PASSES4.95s with exact definitions/metadata/constraint.
Do not revisit those repairs or add product scope. Historical reports remain
immutable; the new report must correct the omitted-proof completion claim.

## Three exact remaining targets

1. **Execute the omitted page-style visual proof.** `git diff a65d184...d2bc4fb`
   has NO change to `tests/e2e/preview.spec.ts`; the078-w page-style browser
   block remains unchanged. Its default-only negative control still occurs
   BEFORE page-style creation and tests theme alone. Implement the explicit078-x
   requirement in that existing same-workspace test:
   - after a real capability writes distinguishable page overrides, intercept
     only the deliberate negative fixture to return inherited-only page output;
     the SAME computed-style checker must reject it;
   - remove the interception, reload real NGINX preview and pass; the successful
     path must never inject DOM/classes or seed claimed outcomes with owner SQL;
   - leave some tokens inherited and some overridden, change the site theme
     through the separately authorized public Agent operation, and observe
     computed inherited changes with explicit overrides unchanged;
   - reset an explicit token and observe resumed inheritance; compare another
     page, workspace/site and canonical against their previous values;
   - retain the existing component-local/responsive precedence and private
     preview assertions. No new feature or giant browser scenario.
   Do not cite the unchanged older theme negative control as this proof again.
2. **Make the concurrent-theme barrier real.** In
   `test_page_style_reset_serializes_with_concurrent_theme_change`, both
   `_wait_for_advisory_waiters` calls return when ANY waiter exists. The second
   call can therefore see only the first/theme waiter, release995 before reset
   reaches it, and pass without the claimed overlap. Require two distinct
   intended blocked backend requests (or an equally deterministic explicit
   barrier) before releasing. Assert completed results/constraint destination
   and no residue. Preserve existing lock order; repair a concrete production
   defect only if this strengthened execution reveals one. Polling pg_locks is
   acceptable; elapsed sleeps as proof are not.
3. **Exercise cancellation of the public style request while waiting.** X's
   post-DML direct-runtime test is useful and remains. It does not exercise
   cancellation of the public mutation executor's prelocked session/reservation
   path. Hold the appropriate DB barrier, start an actual Agent HTTP style
   request, prove it is blocked, cancel that request, and assert unchanged raw
   state/version/quota/COW/idempotency/audit plus no leaked session lock. Reuse
   the pool and same idempotency key successfully afterward. Keep this focused,
   not another broad cancellation framework or production test shortcut.

Before reporting COMPLETE, verify that all three targets have actual executable
assertions in the diff, were run, and passed. No prose-only report, borrowed
adjacent result, or silent omission. If a concrete technical/external blocker
prevents one, report exactly that blocker and unfinished target instead.

## Delivery and frozen scope

Current PR52files,+5754/-115; production/config19, migration1, verification17,
generated1, docs3, OAP11. Growth is mostly tests/transcript; preserve this single
page-style boundary. No globals/header-footer/catalog/media/MCP/lifecycle/
publication/dependencies/exceptions/unrelated cleanup or new PR. Numeric078
remains PARTIAL, only bounded increment can be accepted. Onlyzremains afterward;
do not invent IDs or dilute requirements to fit the alphabet.

Use focused modified tests, then normal required CI; no redundant35-minute
local broad suite by habit. Own routine safe passwordless-sudo setup; no new or
replacement agent. Repair concrete in-scope failures within this round. Update
current PR/truth surfaces honestly, retaining all historical orders/reports.
Correct the existing report's browser-proof omission explicitly, without
claiming prior assertions executed. Read back the actual PR body.

Commit this exact order and active unchanged; push existing branch. Publish
`oap/reports/078-y-finish-page-style-proof.md` as final report-only SELF commit,
parent literal pushed implementation SHA, naming exact assertions/results for
the three targets, PR/base/head, categorized size and current checks. No merge
or auto-merge. Send exact two-byte responseFIFO OK, close, then wait on a REAL
byte-reading control listener. Strategy reviews the whole bounded PR and alone
accepts/merges after complete078-v/w/x/y criteria and exact-head green.
