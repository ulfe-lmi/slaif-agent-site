# OAP Work Order — 087-a (inert until activated)

## Contract and objective

Add the missing approved-source observation tools and quota-controlled
responsive preview sweep required for reconstruction, extending rather than
restarting Objective 072. Links: §§15.8, 23, 24.8, 25.2, 35, 42.4, 52.7.
Requires 074, 080 and 072's merged worker foundation.

## Production requirements

- Add durable SOURCE run contracts and curated Agent REST/MCP `source.open`,
  snapshot, screenshot, links, metadata and bounded asset fetch; arguments are
  relative to immutable human-approved origin policy, never arbitrary URLs.
- Extend the confined browser worker/dispatcher with fresh contexts, separate
  source credentials/egress, DNS+redirect revalidation, default private/
  loopback/link-local/metadata/internal denial, explicit administrator fixture-
  origin allowance, MIME/bytes/pages/download/time limits and private artifacts.
- Add `preview.run_responsive_sweep` over policy-approved stable product
  targets/routes with console/request/link/media/overflow/heading/accessibility
  summaries and quotas; document any runtime engine subset honestly while
  retaining all six architecture E2E projects.
- Worker keeps no DB/content/reviewer/identity authority; observations cannot
  mutate or publish. Extend real MCP named tools, not arbitrary forwarding.

## Acceptance and anti-bypass

Use a real fixture origin through public Agent and MCP, retrieve exact source
evidence, then sweep a real workspace at desktop/tablet/phone and retrieve
private immutable artifacts. Deny unapproved origin, absolute override,
redirect/DNS rebinding to blocked ranges, `file://`, internal service/PG/Docker/
host, credentials in URL, quota excess, cross-site/run/artifact, revoke/expiry,
oversize/MIME/timeouts; prove cleanup/context isolation and no canonical change.
Tests fail if Playwright execution is removed; HTML fixtures alone are not
source-tool proof. Run network-policy, real browser, public NGINX/MCP, restart,
full Compose/supply-chain/CI. Report
`087-a-source-tools-responsive-sweep.md` with SELF; no merge/extra PR.
