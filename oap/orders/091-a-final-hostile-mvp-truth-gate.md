# OAP Work Order — 091-a (inert until activated)

## Contract and objective

Perform the final checked-out-product hostile MVP gate against every material
Revision 2.1 §51.1/§52 requirement and correct all public status documentation.
Requires 073–090 merged; strategy refreshes exact state before activation.

## Required audit and evidence

- Re-evaluate every row in `oap/MVP-CONTRACT-AUDIT.md` against remote `main`,
  production code, public interfaces, tests and fresh runtime evidence; helper,
  report or green-check presence is not enough.
- Run clean Compose, real external Agent REST and MCP semantics, COW isolation,
  destructive authority, Puck/shared rendering, media, real browser/source/
  responsive loop, immutable freeze/review, accept/discard/promotion, conflict,
  News, reconstruction, scheduler/GC, backup/restore, six stable Playwright
  projects, security/privilege/site negatives, license/SBOM and all CI.
- Verify every major E2E would fail if its production feature were replaced by
  fake success; inspect for SQL/ORM/internal/privileged/test-helper bypasses.
- Recheck browser vulnerability exceptions against official qualified metadata,
  issue, expiry and current scan; remove/upgrade when triggered or stop for a
  new explicit unexpired human decision.
- Update audit matrix, `MVP-PROGRESS.md`, README, roadmap/post-MVP, docs and
  known limitations with exact merged PR/test evidence. No percentages as
  primary truth and no production/certification/hostile-SaaS overclaim.

## Binary outcome

If any contractual row lacks strong evidence, report `PARTIAL`, leave MVP not
complete and identify the exact missing production work; do not weaken the
architecture or tests. `MVP COMPLETE` is permitted only when every required row
is `COMPLETE — E2E PROVEN` on verified remote `main`, all required checks pass,
and no human risk/release blocker remains. This objective may repair only
truth/evidence-harness defects; missing product behavior requires a strategic
continuation/new prerequisite.

Use one PR, no release/merge by coding. Publish
`oap/reports/091-a-final-hostile-mvp-truth-gate.md` with exact commands,
artifacts, row-by-row verdict, implementation SHA and SELF.
