# OAP Work Order — 078-x: close specific remaining page-style proof gaps

## Identity and current decision

Objective078, increment078/4, round078-x, AMEND_EXISTING_PR81
<https://github.com/ulfe-lmi/slaif-agent-site/pull/81>, same branch
`oap/078-4-page-style-overrides`. Base/current remote main remains
`fe31c9f30a7797d0916ad7f8fb56344bc61526f3`.
Verified report-only head `a65d184674d34261be8cc681aaccbd8f3edeb76d`, parent
`51e2a78b1fe5c6ca5d4a74a151e039e496157a28`; all20exact-head checks successful.
Genuine078-w FIFO response received; PR is OPEN, not accepted or merged.

The resource predicate, Agent null-version rejection, exact-token reset overlap
and OpenAPI authority repairs are present and must be preserved. Distinguishable
page-style computed output and real process restarts now exist. Do not reimplement
them or add functionality. This order targets remaining explicit078-v/w criteria,
not general strengthening or new scope. Historical orders/reports stay immutable.

## Failed fresh-baseline proof: precise diagnosis

`test_page_style_066_round_trip_preserves_legacy_data_and_blocks_loss` calls
`_seed`, which upgrades to head066, then downgrades065. That is NOT the fresh065
baseline required by078-w. It does not compare the replaced legacy function
definitions or their original metadata across upgrade/downgrade. The report's
fresh-baseline/exact-restoration claim is unsupported by those assertions.

Strategy independently ran a genuinely fresh fixture: run_migration upgrade065,
capture functions+metadata+audit constraint, upgrade066, downgrade065. The test
FAILED in4.81s: the13-argument `control.slaif_agent_idempotency_complete` and
10-argument `_complete_no_effect` definitions differ. Further comparison confirms
ONLY WHITESPACE drift, not a proven semantic/privilege regression; metadata and
constraint match. Do not inflate this into a security incident or redesign.
The exact-restoration contract can be met by preserving the original formatting
in the unmerged066 restoration strings. No merged migration edits.

Diagnostic path, strategy-owned, do not edit:
`/home/ubuntu/codex-supervision/slaif-agent-site/workorders/test_078w_migration_review.py`.
Create a genuine fresh065 data-bearing repository test BEFORE any066 application,
capture definitions/owner/ACL/volatility/config/constraint, prove066→065 exact
restoration, then re-upgrade and assert working066 behavior. Keep existing
pending-COW/data guards. Add the isolated style-audit-only downgrade guard case
and verify rejected downgrade leaves revision/schema/COW/data unchanged.

## Missing cancellation and actual concurrency assertions

No cancellation occurs anywhere in `test_agent_page_style.py`: its lifecycle/
theme barrier test merely waits, releases, and accepts200. Adjacent cancellation
tests cannot close this criterion. Add a named changed-style cancellation test
that reaches a deterministically observed database barrier, cancels the actual
request/transaction, and proves no leaked lock, raw style/version change,
mutation quota, COW operation, idempotency residue or semantic audit. Include
rollback after actual style DML has run but before completion/commit, using a
controlled test barrier or transaction cancellation, not a production shortcut.
Retry/reuse the pool successfully afterward. No sleeps as proof.

The structural-race helper currently always queues style first and sorts outcome
codes. Exercise both meaningful orderings against page move/delete/update,
checking actual winner state/route/style/audit rather than only status sets.
For theme, holding995 then releasing it proves waiting, not a concurrent
theme mutation/reset result. Run real changed theme PATCH versus style reset
with deterministic ordering, proving resolved destination constraints and
returned values share a valid serialization point. Inspect the complete lock
order INCLUDING prelocked_cow_session/snapshot/reservation before066's995 lock;
do not assume adding995 inside the SQL function is sufficient. This is a review
hypothesis to prove or repair, not an asserted new defect. Keep lifecycle280
outermost, no row-lock conversion deadlocks, no generic locking refactor.

## Finite missing authority/accounting and visual assertions

Close these named holes with focused assertions rather than another blanket
suite statement:

- Read-only set-to-current-inherited and reset-explicit-to-equal-inherited must
  be denied as RAW changes; allowed raw no-effect remains pure. Check COW
  operation count and exact audit capability/delegator/operation identity, not
  only aggregate mutation/audit counts.
- Direct runtime narrowed-scope/no-effect resource denial, forbidden human/raw
  helper EXECUTE, and route/root/locale restrictions. Existing resource coverage
  proves route_prefix but not all named bounds. Prove denied reset destination
  and denied typography-family/token selection, not only allowed reset.
- Expired/frozen/revoked style PATCH denial, not GET-only checks. Invalid/null/
  duplicate/unknown reset input must fail stably at the trusted SQL boundary.
- `preview.spec.ts` now proves distinct page styles, but its wrong-output
  negative control occurs BEFORE page overrides, for theme alone. Apply the
  same checker to deliberately wrong inherited-only page output, require it to
  reject, restore the real response and pass with no DOM injection. Change site
  theme while a page has both inherited and explicit tokens: computed inherited
  values follow, explicit values remain, reset resumes inheritance. Assert
  another page/canonical/workspace/site stay unchanged. Retain responsive/local
  precedence. Do not add another giant browser scenario; extend focused proof.
- Human controls currently set all tokens and retain them through Puck. Add
  the mixed same-group update/reset path and crafted unauthorized Editor style
  PATCH denial. No new UI feature or Agent-workspace Puck selection.

## Execution and reporting discipline

This is proof closure on the SAME semantic PR, not a new feature round. Current
PR50files,+4421/-115 includes19production/config,1migration,17verification,
1generated,3docs,9OAP. Its runtime scope remains bounded page-style. Add no
globals/header-footer/catalog/media/MCP/lifecycle/publication, dependencies,
security exceptions, unrelated cleanup, or new PR. Numeric078 stays PARTIAL.

Run focused changed tests first and use required CI for the broad matrix; do not
repeat a35-minute local full integration suite merely by habit. Own routine
passwordless-sudo test setup. Repair concrete in-scope failures within this
round; do not return a no-op or claim absent assertions as complete. If blocked,
name the actual technical/external blocker and exact unfinished criterion.

Update CURRENT truth/PR body and explicitly correct078-w's unsupported
fresh-baseline/cancellation claims in the new report; never rewrite history.
The final report must map each section above to the exact test/assertion/result.
Do not call recreation of an ASGI client a process restart; the existing Compose
restart evidence is genuine and should be cited accurately. A rerun is evidence
of a pass, not proof of an unexplained failure's cause.

Commit this exact order and active unchanged; push existing branch; publish
`oap/reports/078-x-page-style-final-proof.md` as final report-only SELF child of
the literal pushed implementation SHA. Read back PR description and exact-head
checks. No merge/auto-merge/new agent. Send exact two-byte responseFIFO OK and
close, then use a REAL byte-reading control listener. Strategy independently
reviews the entire bounded increment and merges only if all078-v/w/x criteria
and current required checks genuinely pass. No later078scope enters PR81.
