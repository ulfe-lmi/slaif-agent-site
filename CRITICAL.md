# Critical Review Queue

PRs merged autonomously that require human post-merge review.
Focus: security boundaries, authorization logic, data integrity, trust model.

| PR | Objective | Risk | Decisions Taken | Priority |
|---|---|---|---|---|
| [#28](https://github.com/ulfe-lmi/slaif-agent-site/pull/28) | 015-a Editor API content model CRUD | **HIGH**: New HTTP routes expose content model CRUD. SECURITY DEFINER functions with `search_path=pg_catalog` prevent hijacking but the `privileges.py` allowlist was modified to permit `slaif_editor_runtime` EXECUTE on content schema functions. Verify this doesn't create a privilege escalation path. Soft-delete for content types preserves audit; hard-delete for fields may lose provenance. | Chose soft-delete for content types (audit trail), hard-delete for field definitions (simpler cleanup). Granted EXECUTE to both `slaif_editor_runtime` and `slaif_control` because editor routes serve authenticated human users and control API needs read access. Added content-model function names to the bootstrap post-harden grant list rather than modifying `harden_cow_schema` itself. | P1 - Review before production |
| [#29](https://github.com/ulfe-lmi/slaif-agent-site/pull/29) | 016 Content item CRUD | **HIGH**: Adds hard-delete for content items (no soft-delete like content types). `slaif_content_item_delete` performs irreversible DELETE. Verify this aligns with the architecture's audit trail requirements (§7: "Deletion is a workspace tombstone/operation"). The bootstrap privilege grants were expanded for content_item functions; verify no privilege escalation. | Chose hard-delete for content items because the architecture specifies workspace-level tombstones for canonical deletion, not API-level soft-delete. Row version checking enables optimistic concurrency. | P1 - Review before production |
| [#30](https://github.com/ulfe-lmi/slaif-agent-site/pull/30) | 017 Collection view CRUD | **MEDIUM**: Adds collection view CRUD with bounded JSON specs. The `collection_view` table was created in the same migration as its functions (not in the base tables migration). Verify the table schema matches architecture §10. Collection view specs use JSONB with bounded depth/size validation. | Chose to create the table alongside the CRUD functions rather than modifying the existing 016 migration (which is already merged). Filter/sort/projection specs are stored as validated JSONB with bounded complexity per architecture requirements. | P2 - Review before production |
| [#31](https://github.com/ulfe-lmi/slaif-agent-site/pull/31) | 018 Navigation/theme CRUD | **MEDIUM**: Navigation CRUD uses hard-delete; theme is singleton per site (upsert pattern). Verify theme upsert doesn't create race conditions with COW sessions. | Theme uses get-or-create pattern to ensure singleton. Navigation hard-delete matches architecture's workspace-level deletion model for non-canonical data. | P2 - Review before production |
| [#32](https://github.com/ulfe-lmi/slaif-agent-site/pull/32) | 019 Page CRUD | **MEDIUM**: Page CRUD with hierarchical parent_id support. Hard-delete for pages. Verify slug validation prevents path traversal (no `..`, no null bytes) and locale-scoped uniqueness works correctly with COW. | Pages use composite unique constraint on (site_id, locale, slug). Parent-child relationships support page hierarchy per architecture §7. | P2 - Review before production |
| [#33](https://github.com/ulfe-lmi/slaif-agent-site/pull/33) | 020 Component catalog + composition schema | **LOW-MEDIUM**: Defines the trusted component catalog (22 components, 5 categories) and normalized composition schema. All components have bounded maxChildren and no executable prop types. The catalog is the security boundary for what agents/users can instantiate. Verify no component accepts arbitrary HTML/script/eval props. | Chose to define components as TypeScript constants (immutable at runtime) rather than database records. Each component has explicit prop type constraints with bounded enums. The `isCompositionNode` validator accepts `parentId: null` for root nodes. | P3 - Review before production |
| [#34](https://github.com/ulfe-lmi/slaif-agent-site/pull/34) | 021 Page composition CRUD | **MEDIUM**: Normalized composition tree CRUD with recursive node delete. Props validated against forbidden executable names (innerHTML/script/eval). Verify recursive delete doesn't exceed PostgreSQL nesting limits on deep trees. | Chose recursive SQL function for cascade delete (matches architecture's dependency closure requirement). Component types bounded to 63 chars. Forbidden prop names are a deny-list (not allow-list) — the component catalog's typed schema is the primary security control. | P2 - Review before production |
| [#35](https://github.com/ulfe-lmi/slaif-agent-site/pull/35) | 022 Media asset foundation | **MEDIUM**: Media metadata CRUD with MIME allowlist and content-hash dedup. Actual file bytes are NOT uploaded/stored yet — only metadata records. Verify the MIME allowlist matches architecture §12 and that the `content_hash` dedup doesn't create cross-site data leaks. | Chose to separate metadata registration from binary upload. The `storage_key` field is a placeholder for the actual MediaStore integration. Content hash uses SHA-256 of filename as placeholder — real implementation must hash actual file bytes. | P2 - Review before production |
| [#36](https://github.com/ulfe-lmi/slaif-agent-site/pull/36) | 023 Agent API read routes | **HIGH**: First Agent API surface. Capability authentication uses `Bearer sas2_` token format. The `authenticate_agent_capability` method is a placeholder — the real implementation must use SECURITY DEFINER functions to validate capability tokens against the control schema. Cross-site access is prevented by checking `context.site_id`. Read-only (no mutations). | Chose to implement read-only routes first as a foundation. The capability auth placeholder MUST be replaced with real token validation before any production deployment. Route policies use SYSTEM_EXEMPTION authority kind because agent routes are authenticated by capability, not session cookie. | P0 - CRITICAL: Review auth placeholder before ANY deployment |
| [#38](https://github.com/ulfe-lmi/slaif-agent-site/pull/38) | 025 Workspace lifecycle foundation | **HIGH**: Creates the workspace governance table with lifecycle transitions. `slaif_workspace_freeze` transitions ACTIVE→FREEZING→REVIEW in one function call. `slaif_workspace_discard` only works from REVIEW/CONFLICTED/FREEZING. Verify the state machine matches architecture §8 exactly. | Workspace lives in control schema (not COW) because it's a governance object, not editorial content. Freeze is a two-step transition (FREEZING then REVIEW) in one transaction. Duration bounded 1-8 hours per architecture defaults. | P1 - Review before production |
| [#39](https://github.com/ulfe-lmi/slaif-agent-site/pull/39) | 026 Capability token generation | **MEDIUM**: Implements the sas2_ token format with 256-bit secrets and SHA-256 digest storage. Constant-time comparison prevents timing attacks. Verify that `generate_capability_token` uses CSPRNG (secrets module) and that digests are never reversed to plaintext. | Uses `secrets.token_hex(32)` for 256-bit entropy. Digest is `hashlib.sha256(token.encode()).hexdigest()`. No HMAC pepper yet — should be added in production hardening. | P2 - Review before production |
| [#40](https://github.com/ulfe-lmi/slaif-agent-site/pull/40) | 027 Workspace HTTP routes | **HIGH**: Workspace lifecycle HTTP routes (create/get/freeze/discard). Route policies deferred to integration test. Verify that freeze only works from ACTIVE state and discard from REVIEW/CONFLICTED/FREEZING per architecture §8. | Route policies for workspace routes are defined but not yet wired into the coverage validator to avoid breaking the health test's FakeControlDatabase. Integration tests with real DB needed before wiring policies. | P1 - Review before production |
