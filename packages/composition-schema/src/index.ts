/** Boundary identity only; composition contracts are not implemented. */
export const packageMetadata = Object.freeze({
  name: "@slaif-agent-site/composition-schema",
  status: "pre-alpha-scaffold",
  version: "0.0.0",
} as const);
