/** Boundary identity only; component catalog contracts are not implemented. */
export const packageMetadata = Object.freeze({
  name: "@slaif-agent-site/component-catalog",
  status: "pre-alpha-scaffold",
  version: "0.0.0",
} as const);
