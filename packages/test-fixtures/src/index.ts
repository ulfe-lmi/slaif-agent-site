/** Boundary identity only; shared test-fixture data is not implemented. */
export const packageMetadata = Object.freeze({
  name: "@slaif-agent-site/test-fixtures",
  status: "pre-alpha-scaffold",
  version: "0.0.0",
} as const);
