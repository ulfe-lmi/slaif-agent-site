/** Boundary identity only; content-model contracts are not implemented. */
export const packageMetadata = Object.freeze({
  name: "@slaif-agent-site/content-model-schema",
  status: "pre-alpha-scaffold",
  version: "0.0.0",
} as const);
