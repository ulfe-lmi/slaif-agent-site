/** Boundary identity only; browser-tool contracts are not implemented. */
export const packageMetadata = Object.freeze({
  name: "@slaif-agent-site/browser-tool-contracts",
  status: "pre-alpha-scaffold",
  version: "0.0.0",
} as const);
