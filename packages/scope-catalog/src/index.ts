/** Boundary identity only; scope catalog contracts are not implemented. */
export const packageMetadata = Object.freeze({
  name: "@slaif-agent-site/scope-catalog",
  status: "pre-alpha-scaffold",
  version: "0.0.0",
} as const);
